DATABASE_URL=postgresql://user:password@host:5432/scalemax
JWT_SECRET=your_jwt_secret_here
PORT=3001
# Required to sign up as a strategist. Frontend posts a passcode field on
# /auth/register when role === 'strategist'; backend rejects 401 if it
# doesn't match this value.
STRATEGIST_PASSCODE=change_me
